package com.mycompany.lab7.q3;
public class Undergraduate extends Student {
    
    public void display();
    
    //if class is final we cannot create child classes for it
    //if variables are final we cannot assign another values for it
    //if method is final we cannot override it
    
}
